<Window x:Class="OGPSLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OGPS Launcher"
        Height="720" Width="1280"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#0F1014"
        >

    <Window.Resources>
        <!-- Colors -->
        <Color x:Key="CardColor">#28303A</Color>
        <Color x:Key="CardAccent">#2EC76F</Color>
        <SolidColorBrush x:Key="CardBrush" Color="{StaticResource CardColor}" />
        <SolidColorBrush x:Key="LightText" Color="#E6EEF6"/>
        <SolidColorBrush x:Key="SubText" Color="#A6B0C0"/>

        <!-- Shadow effect for floating cards -->
        <DropShadowEffect x:Key="CardShadow" BlurRadius="18" ShadowDepth="6" Color="#000000" Opacity="0.45"/>

        <!-- Play button hover animation -->
        <Style x:Key="PlayButtonStyle" TargetType="Button">
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Background" Value="#28C76F"/>
            <Setter Property="Foreground" Value="#07120A"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="54"/>
            <Setter Property="Width" Value="260"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="10" >
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#6EEAA3" BlurRadius="20" ShadowDepth="0" Opacity="0.7"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="RenderTransform.(ScaleTransform.ScaleX)" Value="1.04"/>
                                <Setter Property="RenderTransform.(ScaleTransform.ScaleY)" Value="1.04"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="RenderTransform.(ScaleTransform.ScaleX)" Value="0.98"/>
                                <Setter Property="RenderTransform.(ScaleTransform.ScaleY)" Value="0.98"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Small round icon button -->
        <Style x:Key="RoundIconButton" TargetType="Button">
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="46"/>
            <Setter Property="Background" Value="#2B2F38"/>
            <Setter Property="BorderBrush" Value="#00000000"/>
            <Setter Property="Foreground" Value="{StaticResource LightText}"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse Fill="{TemplateBinding Background}" />
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.92"/>
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.05" ScaleY="1.05"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid>
        <!-- Background GIF (loaded from code-behind into BackgroundImage) -->
        <Image x:Name="BackgroundImage" Stretch="UniformToFill" Panel.ZIndex="0" />

        <!-- Dim overlay to increase contrast -->
        <Rectangle Fill="#88000000" Panel.ZIndex="1"/>

        <!-- Main layout -->
        <Grid Margin="18" Panel.ZIndex="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="380"/> <!-- left screenshot column -->
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>   <!-- right content -->
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="96"/> <!-- bottom control bar -->
            </Grid.RowDefinitions>

            <!-- LEFT: screenshot column -->
            <Grid Grid.Column="0" Grid.Row="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Big screenshot card -->
                <Border CornerRadius="10" Background="{StaticResource CardBrush}" Padding="8" Effect="{StaticResource CardShadow}">
                    <Image x:Name="UpdateScreenshot" Stretch="UniformToFill" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" />
                </Border>

                <!-- Play button & small status under screenshot -->
                <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Center" Margin="0,12,0,0">
                    <Button x:Name="MainButton"
                            Style="{StaticResource PlayButtonStyle}"
                            Content="▶ Play"
                            Click="MainButton_Click"
                            IsEnabled="False"/>
                    <!-- small status text under play, link to existing StatusText in .cs -->
                    <TextBlock x:Name="StatusTextUnderPoster"
                               Text="{Binding ElementName=StatusText, Path=Text}"
                               Foreground="{StaticResource SubText}"
                               FontSize="12"
                               Margin="0,8,0,0"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>

            <!-- RIGHT: big content area -->
            <Grid Grid.Column="2" Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="220"/> <!-- rightmost column for patches + links -->
                </Grid.ColumnDefinitions>

                <!-- Center-left: News (top) and What's New (big) -->
                <StackPanel Grid.Column="0" Orientation="Vertical" Margin="0,0,12,0">
                    <!-- News (top narrow) -->
                    <Border CornerRadius="8" Background="#333842" Padding="10" Margin="0,0,0,10" Effect="{StaticResource CardShadow}">
                        <StackPanel>
                            <TextBlock Text="NEWS" FontSize="16" FontWeight="Bold" Foreground="{StaticResource LightText}" />
                            <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="80" Margin="0,8,0,0">
                                <TextBlock x:Name="NewsText" TextWrapping="Wrap" Foreground="{StaticResource SubText}" FontSize="14"/>
                            </ScrollViewer>
                        </StackPanel>
                    </Border>

                    <!-- What's New (big) -->
                    <Border CornerRadius="8" Background="#333842" Padding="12" Margin="0,0,0,10" Height="350" Effect="{StaticResource CardShadow}">
                        <StackPanel>
                            <TextBlock Text="WHAT'S NEW" FontSize="18" FontWeight="Bold" Foreground="{StaticResource LightText}" Margin="0,0,0,8"/>
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <TextBlock x:Name="WhatAddedText" TextWrapping="Wrap" Foreground="{StaticResource SubText}" FontSize="14"/>
                            </ScrollViewer>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Right column: Patch notes and vertical links -->
                <StackPanel Grid.Column="1" Orientation="Vertical">
                    <!-- Patch notes card -->
                    <Border CornerRadius="8" Background="#333842" Padding="10" Margin="0,0,0,10" Height="250" Effect="{StaticResource CardShadow}">
                        <StackPanel>
                            <TextBlock Text="PATCH NOTES" FontSize="16" FontWeight="Bold" Foreground="{StaticResource LightText}" />
                            <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,8,0,0">
                                <TextBlock x:Name="PatchNotesText" TextWrapping="Wrap" Foreground="{StaticResource SubText}" FontSize="14"/>
                            </ScrollViewer>
                        </StackPanel>
                    </Border>

                    <!-- Spacer -->
                    <Rectangle Height="8" Fill="Transparent"/>

                    <!-- Social links block (vertical) - replaces logo, does not overlap content -->
                    <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch">
                        <TextBlock Text="Links" Foreground="{StaticResource SubText}" FontSize="12" Margin="6,0,0,6"/>
                        <Button x:Name="DiscordButton" Click="DiscordButton_Click" Style="{StaticResource RoundIconButton}" Margin="0,4,0,4" ToolTip="Discord">
                            <Image Width="22" Height="22" Source="https://cdn-icons-png.flaticon.com/512/2111/2111370.png"/>
                        </Button>
                        <Button x:Name="WebsiteButton" Click="WebsiteButton_Click" Style="{StaticResource RoundIconButton}" Margin="0,4,0,4" ToolTip="Website">
                            <Image Width="20" Height="20" Source="https://cdn-icons-png.flaticon.com/512/841/841364.png"/>
                        </Button>
                        <Button x:Name="GitHubButton" Click="GitHubButton_Click" Style="{StaticResource RoundIconButton}" Margin="0,4,0,4" ToolTip="GitHub">
                            <Image Width="20" Height="20" Source="https://cdn-icons-png.flaticon.com/512/733/733609.png"/>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Grid>

            <!-- BOTTOM CONTROL BAR (row 1) -->
            <Border Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="1" Background="#2A2E35" CornerRadius="6" Padding="12" Effect="{StaticResource CardShadow}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Play (duplicate small) kept on left for visibility (but main Play button already present) -->
                    <Button Grid.Column="0" Content="▶ Play" Click="MainButton_Click"
                            Style="{StaticResource PlayButtonStyle}" Height="44" Width="160" />

                    <!-- Progress area -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                        <ProgressBar x:Name="InstallProgressBar" Width="520" Height="12" Visibility="Collapsed" Margin="8,0,8,0" Foreground="#2EC76F"/>
                        <TextBlock Text="{Binding ElementName=StatusText, Path=Text}" Foreground="{StaticResource LightText}" VerticalAlignment="Center" Margin="8,0,0,0"/>
                    </StackPanel>

                    <!-- Menu button -->
                    <Button x:Name="MenuButton" Grid.Column="4" Content="⋮" Width="42" Height="42" Click="MenuButton_Click" Style="{StaticResource RoundIconButton}" />
                </Grid>
            </Border>

        </Grid>

        <!-- Popup menu (preserved) -->
        <Popup x:Name="OptionsMenu" StaysOpen="False" Placement="Bottom" PlacementTarget="{Binding ElementName=MenuButton}">
            <Border Background="#1F1F1F" BorderBrush="#333" BorderThickness="1" CornerRadius="6" Padding="8">
                <StackPanel>
                    <Button Content="Open game folder" Click="OpenFolderFromMenu_Click" Background="Transparent" Foreground="White" BorderThickness="0" HorizontalContentAlignment="Left"/>
                    <Button Content="Uninstall game and settings" Click="UninstallFromMenu_Click" Background="Transparent" Foreground="#FF6B6B" BorderThickness="0" HorizontalContentAlignment="Left"/>
                </StackPanel>
            </Border>
        </Popup>

        <!-- Loading overlay (preserved) -->
        <Border x:Name="LoadingOverlay" Visibility="Collapsed" Background="#88000000" Panel.ZIndex="6">
            <Grid>
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="260" Height="14"/>
                    <TextBlock Text="Loading..." Foreground="White" FontSize="14" Margin="0,12,0,0" HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

    </Grid>
</Window>