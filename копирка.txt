<Window x:Class="OGPSLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OGPS Launcher"
        Height="600"
        Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#0E1013">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="80"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель -->
        <Border Background="#1E2126" Grid.Row="0">
            <TextBlock Text="OGPS Launcher"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       Foreground="White"
                       FontSize="22"
                       FontWeight="Bold"/>
        </Border>

        <!-- Центральная часть -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <!-- Новости / Ивенты -->
            <StackPanel Grid.Column="0" Margin="0,0,20,0">
                <TextBlock Text="Новости и события"
                           Foreground="White"
                           FontWeight="Bold"
                           FontSize="18"
                           Margin="0,0,0,10"/>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="NewsPanel" />
                </ScrollViewer>
            </StackPanel>

            <!-- Основной контент -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Button x:Name="SelectFolderButton"
                        Content="Выбрать папку с игрой"
                        Width="220" Height="40"
                        Margin="0,0,0,10"
                        Click="SelectFolder_Click"/>

                <Button x:Name="PlayButton"
                        Content="Play"
                        Width="200" Height="60"
                        FontSize="20"
                        Click="PlayButton_Click"/>

                <!-- Кружок прогресса -->
                <ProgressBar x:Name="DownloadProgress"
                             Width="200" Height="8"
                             Margin="0,10,0,0"
                             Foreground="#3FA9F5"
                             Background="#333333"
                             Visibility="Collapsed"/>
            </StackPanel>
        </Grid>

        <!-- Нижняя панель -->
        <Border Background="#1E2126" Grid.Row="2" VerticalAlignment="Center">
            <TextBlock Text="© 2025 OGPS Team | &amp; Cloud Updates System"
                       Foreground="#999999"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="12"/>
        </Border>
    </Grid>
</Window>